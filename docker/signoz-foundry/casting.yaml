# SigNoz via Foundry — https://signoz.io/docs/install/docker/
# UI: http://localhost:8080 (stop catalog REST while using SigNoz UI, or run catalog on PORT=8081)
apiVersion: v1alpha1
kind: Installation
metadata:
  name: signoz
spec:
  deployment:
    flavor: compose
    mode: docker
